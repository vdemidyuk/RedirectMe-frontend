/*
 * GoPage Messages
 *
 * This contains all the text for the GoPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GoPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GoPage container!',
  },
});
