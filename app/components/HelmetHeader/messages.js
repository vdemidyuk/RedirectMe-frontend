/*
 * HelmetHeader Messages
 *
 * This contains all the text for the HelmetHeader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HelmetHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HelmetHeader component!',
  },
});
